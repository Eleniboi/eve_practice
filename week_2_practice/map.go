package main

/*func main() {

	// map in go lang use key store data using key, it is more easier and faster when accessing a particular value from a list, once the key is used it jump straight to the value
	// if you initialize a map using the 'var' key word the value will be nil and you can't assign a value to it
	//
	inventory := map[string]int{

		"Apples":   10,
		"Banana":   5,
		"cherries": 0,
	}

	shopinglist := "cherries"

	quantity, exist := inventory[shopinglist]

	if !exist {
		fmt.Printf("we don't sell %v here", shopinglist)
	} else if quantity == 0 {
		fmt.Printf("%s is out of stock", shopinglist)
	} else {
		fmt.Printf("still have %d %v left", quantity, exist)
	}

	fmt.Println(inventory) //the value of cherries here will still be 0

	inventory["cherries"] = 7 // here i added 7 to the value of cherries

	fmt.Println(inventory)

}
*/
